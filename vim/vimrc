"
" a moj pliczek vima ;]
"
"
set nocompatible    " hmm ? :)
filetype off

set rtp+=~/.vim/bundle/Vundle.vim
let s:cfg_dir = fnamemodify(resolve(expand($MYVIMRC)), ':h')
call vundle#begin()
execute 'source ' . fnameescape(s:cfg_dir . '/plugins.shared.vim')

" kolorki
Plugin 'ghifarit53/tokyonight-vim'                       " colorscheme: tokyonight fork
Plugin 'nordtheme/vim'                                   " colorscheme: nord

" boxy / nawigacja / szukanie
"Plugin 'roman/golden-ratio'                             " auto-resize windows (alt)

" wyrownywanie / edycja
"Plugin 'AutoClose'                                      " auto-close brackets/quotes

" autocomplete

" syntax
Plugin 'saltstack/salt-vim'                              " SaltStack syntax
"Plugin 'ap/vim-css-color'                               " preview CSS colors inline
"Plugin 'ada.vim'                                        " Ada syntax support
"Plugin 'rodjek/vim-puppet'                              " Puppet syntax support

" framework / domain
Plugin 'exu/pgsql.vim'                                   " PostgreSQL SQL syntax
Plugin 'vim-ruby/vim-ruby'                               " Ruby syntax and indent
Plugin 'tpope/vim-rails'                                 " Rails helpers

Plugin 'kylef/apiblueprint.vim'                          " API Blueprint syntax

call vundle#end()
execute 'source ' . fnameescape(s:cfg_dir . '/vimrc.shared')

set ts=2       " szerokosc tabulacji :)
set sw=2       " szerokosc automagicznego wciecia
"set smarttab - moze uzyc
"set cursorline     " pokazuj zaznaczenie linii - woleeeeee w MacVim

set encoding=utf-8


" Backups
set directory=~/.swp-vim/

" Plugins config {{{

if (has("gui_running"))
"    let g:airline_left_sep=''
"    let g:airline_right_sep=''
"    let g:airline_powerline_fonts=0
"    let g:airline_theme='jellybeans'
endif
" }}}

" golden ration nie rusza tagbar i nerdtree
"let g:golden_ratio_exclude_nonmodifiable = 1
"let g:golden_ratio_filetypes_blacklist = ["nerdtree", "tagbar"]
